import { describe, it, expect } from "vitest";
import { RoleRoute } from "./RoleRoute";

describe("RoleRoute", () => {
  it("exports RoleRoute component", () => {
    expect(RoleRoute).toBeDefined();
  });
});
